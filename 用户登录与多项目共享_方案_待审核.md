# 用户登录 + 多会话录制 — 方案（待审核）

> 目标：用户登录后录制；换另一台设备登录**同一用户**即可查看录制结果；且**同一用户可同时开 3–4 个录制页面（会话）并行录制**。
> 去掉：多项目、多角色（viewer/operator）、复杂权限。只保留「用户」这一层。
> 本文只描述方案，不含代码改动。

---

## 1. 问题本质

当前 `sid` 是**客户端 localStorage 随机生成**且**后端无条件信任**：换设备就是新 sid（看不到旧录制），且任何人传 `?sid=` 就能读他人数据。

加登录的核心：**身份从「客户端随机 sid」改成「服务端按登录用户分配」**，后端不再信任客户端传来的 sid。数据按用户归属，所以同一用户在任何设备都能看到自己全部录制。

---

## 2. 两层命名空间：用户 → 会话

关键点：一个用户要能**并行跑 3–4 个录制**，所以「会话」概念不能取消，而是**收编到用户名下、并对同用户的所有设备可见**（不再藏在某台设备的 localStorage 里）。

```
recordings/<user>/<session>/<base|rover>__<mount>/
                                   ├─ <stamp>.rtcm
                                   ├─ <stamp>.csv
                                   └─ index.jsonl
```

- **user**：登录账号，顶层归属。
- **session**：一次录制（≈ 一个录制页面/一路或一对 NTRIP 流）。同一用户可有多个并行 session。
- 每个 session 内部仍是现有的 `base__挂载点/` `rover__挂载点/` 结构，**完全不用改**。

后端 `State` 由「按 sid 建一份」改为「按 `user/session` 建一份」——逻辑几乎照搬，只是 key 从随机 sid 变成 `user/session`，且这个 key 由**服务端 session + 请求里的 session 参数**得出（并校验 session 属于该 user），不再信任客户端 sid。

---

## 3. 用户/账号存储

- 账号很少时用 **JSON 文件**即可（`users.json`：`{username: {pw_hash}}`）；要管理界面或较多账号再上 SQLite（标准库 `sqlite3`）。
- 密码用 `werkzeug.security.generate_password_hash` / `check_password_hash`。
- 不需要角色字段——登录即对自己名下所有 session 有完整权限。

---

## 4. 鉴权与会话(cookie)

- 新增 `/login`（GET 登录页 / POST 校验）、`/logout`。
- 登录成功写 **Flask server-side session cookie**（用持久化 `SECRET_KEY` 签名）。
- 所有页面与 `/api/*` 加守卫：未登录 → 跳登录 / 401。
- **身份只从 cookie 取**；命名空间 = `当前登录user / 请求带的session`，且校验该 session 属于该 user。

---

## 5. 多会话：列举、新建、并行

新增/调整接口（概念层）：

| 接口 | 作用 |
|------|------|
| `GET /api/sessions` | 列出**当前用户的全部会话** = 内存中在跑的 State + 磁盘 `recordings/<user>/*`；每条带 label、挂载点、是否录制中、可导出时间范围 |
| `POST /api/sessions`（或连接时隐式创建） | 新建一个会话（生成 session id，可带用户自定义 label，如「A区基站」） |
| `/api/connect` `/api/disconnect` `/api/*eph*` | 加 `session` 参数；user 从 cookie 取；对该 user/session 操作 |
| `/api/recordings` `/api/track` `/api/export` | 同样按 `user/session` 取数据，忽略客户端 sid |

**并行 3–4 个录制**：每个录制页面（或浏览器标签）对应一个 session，各自连各自的 NTRIP 流、各写各的 `recordings/<user>/<session>/`，互不干扰。后端本就支持多 State 并存，reaper 已豁免「正在录制」的状态，长时间并行录制安全。

---

## 6. 前端改动（概念层）

- 登录页。
- 登录后首屏是「**我的录制会话**」列表（来自 `/api/sessions`，跨设备一致）：
  - 每条显示 label / 挂载点 / ●录制中·N周期 / 可导出范围；
  - 「打开」进入该 session 的监控+轨迹页（即现有主界面，只是数据按 user/session 取）；
  - 「新建录制」开一个新 session。
- 想并行 3–4 个：开多个标签页，各打开/各新建一个 session 即可。
- localStorage 不再存随机 sid，只可选缓存「这个标签当前打开的 session id」，真值以服务端 `/api/sessions` 为准。

---

## 7. 你的目标场景走查

1. 用户登录 → 新建会话「A基站」→ 连流录制；可再开标签新建「B基站」「C测站」……并行 3–4 路。
2. 换另一台设备，登录**同一用户** → 首屏即看到这 3–4 个会话（含录制中状态）。
3. 点开任一会话 → 在「录制导出/轨迹叠加」里按时段查看回放——**无需导出文件、无需手填 sid**。

---

## 8. 安全必做项（已对外暴露，务必落实）

- **HTTPS/TLS 强制**：登录口令与 NTRIP 凭据现为明文过网，对外必须加密（建议 nginx/caddy 反代 + 证书）。
- 密码哈希存储；持久化 `SECRET_KEY`；cookie 设 `HttpOnly + Secure + SameSite=Lax`。
- 登录失败限速/锁定，防爆破。
- **去掉「信任客户端 sid」这条链**（本方案安全基石）。
- NTRIP 凭据：维持「会话临时传、不落盘」即可；若要录制机断电后自动重连，再考虑按会话加密存储。

---

## 9. 并发小注（可选加固）

- 不同 session 并行：天然独立，无需锁。
- 同一 session 被同用户两台设备同时「连/断流」：可加一个**每会话软锁 + 心跳**（谁在控制谁持锁，另一台只读），避免互相打架。非必需，可二期再加。

---

## 10. 存量数据迁移

- 现有 `recordings/8c20727e-…/`（旧随机 sid）→ 一次性移到 `recordings/<user>/<session>/`（或建 `sid→user/session` 别名映射）。无需重录。

---

## 11. 建议分期

- **第一期**：登录 + cookie 会话 + 全站守卫 + 身份只从服务端取 + HTTPS。现有数据归到默认用户/默认会话，先把「安全 + 同用户跨设备查看」跑通。
- **第二期**：`/api/sessions` 列举 + 新建会话 + 前端会话列表/选择器；支持并行多会话；目录迁移到 `user/session`。
- **第三期（可选）**：每会话软控制锁、账号管理界面。

> 请确认：①账号存储 JSON 够用还是要 SQLite；②会话是否需要用户自定义 label（还是自动编号即可）；③是否需要第 9 节的软控制锁。确认后进入编码。
