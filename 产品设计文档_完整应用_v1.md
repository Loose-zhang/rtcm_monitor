# RTCM Monitor — 完整应用产品设计文档（PDD）

> 版本 v1.0 · 状态：待审核 · 作者：产品/技术 · 日期：2026-06-19
>
> 本文从 PM 视角描述把现有「单机实时监控工具」演进为「带账号体系、数据库、可多设备协作的完整应用」的设计方案。
> 范围决策（已与需求方确认）：**面向个人 / 小团队自托管**；后端做**分层重构**；数据库承载**用户·会话·权限 / 录制元数据索引 / 设备·挂载点·基站台账 / 告警·事件历史**四类数据；本文不含具体代码，只描述设计与分期路线。

---

## 1. 背景与问题陈述

### 1.1 现状
RTCM Monitor 当前是一个 Flask 单体 + 单文件前端的**实时监控工具**：接入 NTRIP 流或回放 `.rtcm`，解码 RTCM3 MSM 观测值，做频点 / CN0 可视化、真实天空图、北斗 PRN 重构校验，并已实现**双通道（基站 / 测站）录制与按时段导出**。落盘结构为 `recordings/<sid>/<role>__<mount>/`。

### 1.2 核心痛点
当前 `sid` 由**前端 localStorage 随机生成、后端无条件信任**，由此产生三个结构性问题：

1. **无身份**：换一台设备就是新 `sid`，看不到自己此前的录制；无法多设备协作。
2. **不安全**：任何人构造 `?sid=` 即可读取他人录制数据；NTRIP 凭据、登录信息明文过网。
3. **难管理**：录制 / 设备 / 基站坐标 / 告警全部散落在文件系统与内存，无法检索、统计、回溯。

### 1.3 本次目标（一句话）
**把"身份"从客户端随机 sid 上移到服务端账号体系，并引入数据库作为元数据与配置的单一可信源，使同一用户在任意设备登录后都能安全地管理、并行运行、回溯自己的全部监控会话。**

---

## 2. 目标与非目标

### 2.1 目标（In Scope）
- 账号体系：注册（管理员建号）/ 登录 / 登出 / 密码哈希存储 / 服务端会话。
- 数据库化：用户、登录会话、录制会话（监控会话）、录制周期元数据索引、设备 / 挂载点 / 基站台账、告警与事件历史。
- 多设备一致性：同一用户在 A、B 设备登录看到相同的会话列表与录制状态。
- 多会话并行：单用户可同时运行 3–4 路录制（每路独立 NTRIP 流），互不干扰。
- 后端分层重构：API 层 / Service 层 / DAO（仓储）层 / 领域模型分离，便于测试与演进。
- 安全基线：去除"信任客户端 sid"链路、HTTPS 强制、登录限速、安全 Cookie。

### 2.2 非目标（Out of Scope，本期不做）
- 多租户 SaaS、计费、组织隔离（自托管小团队不需要）。
- 复杂 RBAC（viewer / operator / 多级权限）；本期只做"管理员 / 普通用户"两档。
- 更换核心 GNSS 解码 / 天空图算法（沿用现有 `rtcm_decode` / `gnss_orbit`，不重写）。
- 横向扩展、分布式部署、消息队列（单实例足够，留接口不实现）。
- 原始 `.rtcm` 字节入库（仍存文件系统，数据库只存索引与指针）。

---

## 3. 用户与使用场景

### 3.1 角色
| 角色 | 说明 | 权限 |
|---|---|---|
| 管理员（admin） | 自托管的负责人 | 建号 / 改密 / 看全部用户的设备台账与系统状态 |
| 普通用户（user） | 日常监控操作员 | 管理自己名下的会话、录制、设备、告警；不可见他人数据 |

> 本期权限模型刻意从简：以"数据归属（owner_id）"实现隔离，而非细粒度权限矩阵。后续需要再引入角色表与策略。

### 3.2 关键用户故事（User Stories）
1. 作为用户，我登录后能在首屏看到"我的监控会话"列表（含哪几路在录、各自周期数、可导出范围），无需手填 sid。
2. 作为用户，我能新建一个会话「A 区基站」，连上 NTRIP 即开始录制；再开一路「B 区测站」并行录制。
3. 作为用户，我换到另一台电脑登录同一账号，立刻看到上述会话与录制中状态，并能按时段回放 / 导出。
4. 作为用户，我能把常用基站坐标、挂载点存为"台账"，新建会话时一键选用，不必每次手填。
5. 作为用户，我能查看历史告警（北斗重构异常、CN0 掉线、流中断），并按时间 / 会话筛选。
6. 作为管理员，我能新增账号、重置密码、查看全系统正在运行的会话与磁盘占用。

---

## 4. 总体架构

### 4.1 演进策略：分层重构（保留 Flask，重组内部）
在不更换 Web 框架的前提下，把现有"路由里直接操作内存 State + 文件"的写法，重构为清晰的四层。改动可分期、风险可控、对现有解码 / 录制 / 导出模块零侵入。

```
┌─────────────────────────────────────────────────────────────┐
│  前端  static/  (登录页 + 会话列表 + 监控主界面)              │
│         经 HTTPS / Cookie 会话与后端通信                       │
└───────────────▲───────────────────────────────┬─────────────┘
                │ JSON / SSE                     │ 静态资源
┌───────────────┴───────────────────────────────▼─────────────┐
│  ① API 层 (Flask Blueprint)                                   │
│     auth_bp / sessions_bp / recordings_bp / export_bp /      │
│     devices_bp / alerts_bp  —— 只做：鉴权守卫、参数校验、     │
│     调 Service、序列化响应。不含业务逻辑。                     │
├──────────────────────────────────────────────────────────────┤
│  ② Service 层 (业务用例)                                       │
│     AuthService / SessionService / RecordingService /        │
│     ExportService / DeviceService / AlertService             │
│     —— 编排领域逻辑、事务边界、权限归属校验(owner_id)。        │
├───────────────┬──────────────────────────┬───────────────────┤
│  ③ DAO/仓储层  │  ④ 运行时引擎(常驻内存)   │                   │
│   SQLAlchemy   │   ChannelManager:        │                   │
│   Repository   │   {user/session →        │                   │
│   + 文件仓储   │     Channel(NTRIP线程,    │                   │
│   (.rtcm/csv)  │     解码, Recorder)}      │                   │
└───────┬────────┴──────────┬───────────────┴───────────────────┘
        │                   │
   ┌────▼─────┐       ┌──────▼────────────────────────────┐
   │ Database │       │ 文件系统                            │
   │ SQLite   │       │ recordings/<user>/<session>/...    │
   │ (元数据) │       │ exports/<user>/<session>/...       │
   └──────────┘       └────────────────────────────────────┘
```

**两类状态分离是本架构的关键：**
- **持久元数据**进数据库（用户、会话定义、录制周期索引、台账、告警）——可检索、跨设备一致、可回溯。
- **运行时状态**（NTRIP 线程、实时解码内存、SSE 推送）仍常驻进程内存，由 `ChannelManager` 按 `user/session` 管理。数据库不存高频实时数据，只在周期封存 / 状态变更时写一条元数据。

### 4.2 复用与改造边界
| 模块 | 处置 | 说明 |
|---|---|---|
| `rtcm_decode.py` / `gnss_orbit.py` / `freq_table.py` / `bds_recon.py` | **零改动** | 纯算法，作为领域服务被调用 |
| `ntrip_source.py` | 基本不动 | 线程化数据源，由 ChannelManager 持有 |
| `recorder.py` / `exporter.py` | **小改** | 落盘根路径从 `<sid>` 改为 `<user>/<session>`；封存时回调 RecordingService 写一条 DB 索引 |
| `app.py` | **拆分** | 由单文件拆为 `app factory + blueprints + services + repositories + models` |
| `static/index.html` | **增量** | 新增登录页、会话列表页；主界面数据源改为按 user/session 取 |

---

## 5. 数据库设计

### 5.1 选型
**SQLite + SQLAlchemy ORM**。自托管小团队并发低，SQLite 零运维、单文件备份；通过 ORM 抽象，未来切 PostgreSQL 改连接串即可。开启 WAL 模式以改善并发读写。

### 5.2 ER 概览
```
 users ──1:N── monitor_sessions ──1:N── recording_periods
   │                  │                       │
   │                  │                       └─ (指向文件系统 .rtcm/.csv 的路径与统计)
   │                  ├──1:N── channels (base/rover 运行配置)
   │                  └──1:N── alerts
   │
   ├──1:N── devices (基站/测站台账)
   ├──1:N── mountpoints (NTRIP 挂载点台账)
   └──1:N── login_sessions (服务端会话/审计)
```

### 5.3 表结构

**users — 账号**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| username | TEXT UNIQUE NOT NULL | 登录名 |
| password_hash | TEXT NOT NULL | `werkzeug` PBKDF2/scrypt 哈希 |
| role | TEXT DEFAULT 'user' | `admin` / `user` |
| is_active | BOOLEAN DEFAULT 1 | 停用即禁止登录 |
| created_at / updated_at | DATETIME | |
| last_login_at | DATETIME | |
| failed_attempts / locked_until | INTEGER / DATETIME | 登录限速与锁定 |

**login_sessions — 登录会话 / 审计**（可选，便于"在哪些设备登录、强制下线"）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | TEXT PK | 会话 token（服务端签发） |
| user_id | FK users | |
| created_at / expires_at | DATETIME | |
| ip / user_agent | TEXT | 审计 |
| revoked | BOOLEAN | 手动下线 |

**monitor_sessions — 监控会话**（即"一个录制页面 / 一路或一对 NTRIP 流"）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | TEXT PK | 服务端生成，全局唯一 |
| user_id | FK users | **归属，隔离边界** |
| label | TEXT | 用户自定义，如「A 区基站」 |
| status | TEXT | `idle` / `recording` / `stopped` |
| base_xyz | TEXT(JSON) | 基站 ECEF 坐标（来自 1005 或台账） |
| created_at / updated_at | DATETIME | |

**channels — 通道运行配置**（一个会话下 base / rover 两路）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| session_id | FK monitor_sessions | |
| role | TEXT | `base` / `rover` / `eph` |
| source_type | TEXT | `ntrip` / `file` |
| caster_host / port / mountpoint | TEXT / INT / TEXT | NTRIP 连接（**不存密码**，见 §7.4） |
| is_recording | BOOLEAN | |
| last_epoch_at | DATETIME | 健康度 |

**recording_periods — 录制周期索引**（把 `index.jsonl` 入库）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| session_id | FK monitor_sessions | |
| role | TEXT | base/rover |
| mountpoint | TEXT | |
| period_start | DATETIME(UTC) | 整 10 分对齐 |
| rtcm_path / csv_path | TEXT | **文件系统指针**（原始字节仍存盘） |
| sat_count / sys_mask | INT / TEXT | 该历元卫星数、星座掩码 |
| created_at | DATETIME | 封存时刻 |
| 索引 | (session_id, period_start) | 时段检索主路径 |

**devices — 设备 / 基站台账**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| user_id | FK users | |
| name | TEXT | 如「测区 A 基站」 |
| type | TEXT | base / rover |
| ecef_x/y/z | REAL | 基站已知坐标 |
| llh_lat/lon/h | REAL | 经纬高（冗余存储，便于显示） |
| note | TEXT | |

**mountpoints — NTRIP 挂载点台账**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| user_id | FK users | |
| caster_host / port / mountpoint | TEXT/INT/TEXT | |
| username | TEXT | 账号名可存；**密码默认不存**，按需加密存（§7.4） |
| label | TEXT | |

**alerts — 告警 / 事件历史**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| session_id | FK monitor_sessions | |
| user_id | FK users | 冗余，便于按用户查 |
| type | TEXT | `bds_recon`（北斗重构异常）/ `cn0_drop` / `stream_lost` / `eph_missing` |
| severity | TEXT | info / warn / error |
| sat / signal | TEXT | 相关卫星 / 信号 |
| message | TEXT | 人类可读描述 |
| ts | DATETIME | 事件时间 |
| acknowledged | BOOLEAN | 是否已确认 |
| 索引 | (user_id, ts) / (session_id, ts) | |

> 设计原则：**数据库存"指针 + 统计 + 可检索维度"，不存大块二进制**。原始 `.rtcm` / `.csv` 继续走文件系统，DB 仅记录路径与摘要，兼顾检索效率与存储成本。

### 5.4 存量数据迁移
现有 `recordings/<旧随机sid>/` 一次性归入默认管理员账号的一个默认会话：建立 `sid → user/session` 映射，扫描各 `index.jsonl` 回填 `recording_periods`。原始文件**不搬动 / 不重录**，只建索引行（或软链接到新路径）。提供一次性迁移脚本，幂等可重跑。

---

## 6. 鉴权与会话设计

### 6.1 登录流程（时序）
```
浏览器                  API层(auth_bp)        AuthService         DB
  │  POST /login          │                      │                │
  │ (username,password)   │                      │                │
  ├──────────────────────►│  authenticate()      │                │
  │                       ├─────────────────────►│ 查 user        │
  │                       │                       ├───────────────►│
  │                       │                       │ check_hash     │
  │                       │                       │ 失败→记次/锁定  │
  │                       │   成功: 写 server     │                │
  │                       │   session + 签发cookie│                │
  │  302 → / + Set-Cookie │◄─────────────────────┤                │
  │◄──────────────────────┤  HttpOnly+Secure     │                │
```

### 6.2 会话与守卫
- 登录成功写 **Flask server-side session**，用**持久化 `SECRET_KEY`**（落配置文件 / 环境变量，不可每次重启重生成，否则全员掉线）签名。
- Cookie 设 `HttpOnly + Secure + SameSite=Lax`，设合理过期与"记住我"。
- 全站守卫：一个 `@login_required` 装饰器套在所有页面与 `/api/*`（除 `/login`、健康检查、静态登录资源）。未登录 → 页面 302 跳登录、API 返回 401。
- **身份只从 Cookie 取**，请求里的 `session` 参数只用来选"哪一路监控会话"，且 Service 层必校验 `monitor_session.user_id == 当前用户`，否则 403。**彻底废除信任客户端 sid 的链路。**

### 6.3 防爆破
- 同账号连续失败 N 次 → 指数退避锁定 `locked_until`。
- 可选登录验证码 / IP 限速（小团队自托管可仅做账号级锁定）。

---

## 7. API 设计（重构后）

> 约定：所有 `/api/*` 需登录；`user` 一律从 Cookie 取，**不接受客户端传 user/sid**；涉及具体会话的接口带 `session` 参数并做归属校验。

### 7.1 鉴权
| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/login` | 登录页 |
| POST | `/login` | 校验并签发会话 |
| POST | `/logout` | 注销当前会话 |
| GET | `/api/me` | 当前用户信息（用户名 / 角色） |

### 7.2 会话管理（新增）
| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/api/sessions` | 列出当前用户全部监控会话（内存在跑的 + DB 里的），含 label / 挂载点 / 是否录制 / 可导出范围 |
| POST | `/api/sessions` | 新建会话（可带 label，或连接时隐式创建） |
| PATCH | `/api/sessions/<id>` | 改 label / 关联台账 |
| DELETE | `/api/sessions/<id>` | 删除会话（可选保留磁盘数据） |

### 7.3 现有接口的改造（加 `session`、去 `sid`）
| 现状 | 改造后 |
|---|---|
| `/api/connect` `/api/disconnect`（带 role） | 增 `session` 参数；user 从 Cookie；操作 `ChannelManager[user/session]` |
| `/api/connect_eph` `/api/disconnect_eph` | 同上 |
| `/api/recordings` | 按 `user/session` 从 DB `recording_periods` 查（不再扫客户端 sid 目录） |
| `/api/track` `/api/export` `/api/export_zip` `/api/import_track` | 按 `user/session` 取数据，落盘根路径改 `<user>/<session>` |
| `/stream`（SSE） | 推送当前会话快照；连接需登录且校验会话归属 |

### 7.4 台账与告警（新增）
| 方法 | 路径 | 作用 |
|---|---|---|
| GET/POST/DELETE | `/api/devices` | 基站 / 测站台账增删查 |
| GET/POST/DELETE | `/api/mountpoints` | 挂载点台账增删查 |
| GET | `/api/alerts?session=&type=&since=` | 历史告警检索（分页） |
| POST | `/api/alerts/<id>/ack` | 标记已确认 |

**NTRIP 凭据策略**：默认沿用"连接时临时传、不落盘"。若用户勾选"断电自动重连"，则用从 `SECRET_KEY` 派生的密钥**对称加密后**存 `mountpoints.password_enc`，仅该用户会话可解密使用。明文密码永不入库、永不出现在日志。

---

## 8. 前端改动（概念层）

- **登录页**：独立 `login.html`，未登录访问任何页一律跳此。
- **首屏 = 我的监控会话**：来自 `/api/sessions`，跨设备一致。每条显示 label / 挂载点 / ●录制中·N 周期 / 可导出范围；提供「打开」「新建录制」「删除」。
- **监控主界面**：沿用现有巨幕仪表盘（天空图 + 矩阵 + 轨迹叠加 + 抽屉），仅把数据源从"localStorage sid"改为"当前打开的 session"。
- **台账选择器**：新建会话 / 配置基站时，可从 `devices` / `mountpoints` 下拉一键选用。
- **告警中心**：新增页 / 抽屉，列 `alerts` 历史，支持按类型 / 时间 / 会话筛选与确认。
- localStorage 不再存随机 sid，至多缓存"本标签当前打开的 session id"，真值以 `/api/sessions` 为准。
- 并行 3–4 路：开多个标签页，各打开 / 各新建一个 session 即可。

---

## 9. 安全设计（必做基线）

1. **HTTPS / TLS 强制**：登录口令与 NTRIP 凭据当前明文过网，对外部署**必须**经 nginx / Caddy 反代 + 证书；HTTP 一律 301 跳 HTTPS。
2. **去除"信任客户端 sid"链路**——本方案安全基石，身份只认 Cookie。
3. **密码哈希**：`werkzeug` PBKDF2/scrypt，不可逆；禁止明文 / 可逆存储。
4. **会话 Cookie**：`HttpOnly + Secure + SameSite=Lax`，持久 `SECRET_KEY`。
5. **登录限速 / 锁定**：账号级失败计数 + 退避锁定。
6. **归属校验**：每个会话级接口校验 `owner_id`，越权返回 403。
7. **NTRIP 凭据**：默认不落盘；落盘则对称加密；日志脱敏。
8. **路径穿越防护**：导出 / 下载沿用现有 `basename` 校验，DB 指针只在受控根目录内解析。
9. **审计**：`login_sessions` 记录登录设备与时间，支持强制下线。

---

## 10. 分期路线图（PM 视角）

| 期 | 目标 | 主要交付 | 价值 | 风险 |
|---|---|---|---|---|
| **第一期：安全地基** | 登录 + Cookie 会话 + 全站守卫 + 去 sid + HTTPS | users 表、AuthService、`@login_required`、现有数据归入默认用户/会话 | 立刻消除越权读取、实现同用户跨设备查看 | 低（不动解码 / 录制核心） |
| **第二期：会话与数据库化** | `/api/sessions` 列举 / 新建 + 目录迁移到 `user/session` + `recording_periods` 入库 | SessionService、迁移脚本、前端会话列表 | 多会话并行、录制可检索、跨设备一致 | 中（数据迁移需幂等可回滚） |
| **第三期：台账与告警** | devices / mountpoints 台账 + alerts 历史持久化与检索 | DeviceService / AlertService、告警中心前端 | 配置复用、告警可回溯统计 | 中（需把现有内存告警接到持久化管线） |
| **第四期（可选）：协作加固** | 每会话软控制锁 + 心跳 + 账号管理界面 + 审计下线 | 控制锁、admin 管理页 | 多设备同会话不打架、可运维 | 低 |

> **建议先交付第一期**：用最小改动拿到"安全 + 同用户跨设备查看"，验证账号模型后再推进数据库化迁移。

---

## 11. 非功能性需求（NFR）

- **性能**：单实例支撑单用户 3–4 路并行录制；DB 写仅在周期封存（约每 10 分钟 / 路）发生，压力极低。
- **可靠性**：录制中会话豁免 reaper，支持脱机长录；DB 写用事务 + `tmp+rename` 原子落盘（沿用现有 `_atomic_write`）。
- **可维护性**：分层后 Service 可单元测试，DAO 可 mock；API 层薄。
- **可移植性**：ORM 抽象，SQLite→PostgreSQL 仅改连接串与少量方言。
- **可备份性**：DB 单文件 + `recordings/` 目录，定时打包即完成全量备份。
- **可观测性**：结构化日志（脱敏）、`/api/health` 健康检查、磁盘占用统计接口。

---

## 12. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| `SECRET_KEY` 重启重生成 | 全员掉线 / 加密凭据失效 | 持久化到配置 / 环境变量，部署校验项 |
| 存量数据迁移出错 | 历史录制丢失可见性 | 迁移脚本幂等 + 只建索引不删原文件 + 先备份 |
| SQLite 并发写阻塞 | 高并发下偶发锁等待 | 开 WAL；DB 仅低频元数据写；预留 PG 迁移路径 |
| NTRIP 凭据加密存储 | 落盘凭据泄露风险 | 默认不存；存则对称加密 + 最小可见范围 + 日志脱敏 |
| 同会话被两设备同时操控 | 连 / 断流互相打架 | 第四期软控制锁 + 心跳（本期文档先记录，不实现） |
| 重构引入回归 | 现有监控 / 导出功能受损 | 分层渐进、核心算法模块零改动、每期补单元 + 接口测试 |

---

## 13. 待确认事项（Open Questions）

1. 账号是否需要**自助注册**，还是仅管理员建号？（本文按"管理员建号"设计）
2. 会话 label 是**用户自定义**还是自动编号即可？（本文按可自定义）
3. NTRIP 凭据是否需要"断电自动重连"从而**加密落盘**？（默认不落盘）
4. 是否需要第四期的**软控制锁 / 账号管理界面**，还是 CLI 管理账号即可？
5. 告警类型清单是否需要扩展（除北斗重构 / CN0 掉线 / 流中断 / 星历缺失外）？

---

## 附录 A：当前代码与目标结构映射

| 当前 | 目标分层位置 |
|---|---|
| `app.py`（路由+逻辑+内存） | 拆为 `app.py`(factory) + `api/*_bp.py` + `services/*.py` + `repositories/*.py` + `models.py` |
| 内存 `State` / `Channel` | 运行时 `ChannelManager`（按 user/session）；持久部分入 DB |
| `recorder.py` 落盘 `<sid>/` | 落盘 `<user>/<session>/` + 封存回调写 `recording_periods` |
| `exporter.py` 读 `index.jsonl` | 读 DB `recording_periods`（或继续读 jsonl 作为兜底） |
| 前端单文件 | + 登录页 + 会话列表 + 告警中心（增量） |

## 附录 B：术语
- **monitor_session（监控会话）**：一次录制 / 一个监控页面，≈ 一路或一对 NTRIP 流；本文的"会话"默认指此。
- **login_session（登录会话）**：服务端签发的认证态，与上者不同。
- **period（周期）**：UTC 整 10 分钟对齐的录制单元，每周期保留最后一个完整历元。
- **role**：通道角色 base（基站）/ rover（测站）/ eph（星历流）。
