# RTCM Monitor — 双通道 / 天空图 / 周期保留 / 轨迹导出 需求方案（待审核）

> 文档性质：需求与技术方案评审稿。除"天空图 10°/75° 高度角参考线"已直接实现外，本文涉及的功能（项 1–4）**暂不动代码**，待你审核确认后再开发。
> 撰写日期：2026-06-17

---

## 一、背景与现状

当前工具是单设备的 RTCM3 实时监控：一条 NTRIP 观测流 + 一条独立的广播星历流，解码 MSM 观测值，按卫星/信号可视化 CN0 与频点，并用星历 + 基准站坐标算真实方位/高度角画天空图。

与本次需求相关的关键现状（来自代码走读）：

- **单观测通道**。`State` 每会话只持有一个观测源 `source` 和一个星历源 `eph_source`；`State.sats` 是单一卫星字典，没有"设备/角色"维度。
- **天空图标记单一**。`drawSky()` 中所有卫星都画成圆点，点大小 `rad = 3 + (max(20,cn0)-20)*0.35`，颜色按星座 `SYS_COLOR`。真实方位/高度角分支用 `r = R*(90-el)/90` 映射。
- **基准站坐标来自观测流的 1005**。`base_xyz` 解析自 1005，用于算 az/el。测站当前没有独立坐标来源。
- **没有任何落盘/录制**。所有数据都在内存里随 SSE 推送，会话空闲 90 秒即被回收（`SessionManager.reap`），断电/重启后数据全部丢失。
- **连接 API 无"角色"概念**。`/api/connect`、`/api/connect_eph` 各管一条流。

因此项 1–4 不是小改动：项 1 要在 `State` 里引入"通道/角色"维度，项 3、4 要新增一套**录制与持久化**子系统。下面逐项拆解。

---

## 二、需求逐项拆解

### 项 1：新增一条 NTRIP 通道，原通道=基站，新通道=测站

**需求理解**：同时接入两路 NTRIP 观测流。原有那一路定位为"基站(base)"，新增的一路定位为"测站(rover)"。两路各自独立解码、独立统计，在同一界面上对比展示。

**改动点**：
- `State` 重构为按角色组织：把"卫星字典 / 1005 坐标 / 报文计数 / 事件 / 重构标记"等从全局拆成 `base` 与 `rover` 两份子状态（建议抽出 `ChannelState`），`State` 再持有 `channels = {"base": ..., "rover": ...}` 和共享的 `eph`（星历是全局的，两通道共用）。
- 数据源：新增第二个 `NtripSource/FileSource` 实例；`on_frame` 回调要带 role 标识，写入对应子状态。
- 连接 API：`/api/connect` 增加 `role` 参数（base/rover），或新增 `/api/connect_rover`；`start/stop/reap` 都要按通道分别管理。
- 前端：连接区新增一组"测站 NTRIP"表单（与现有 NTRIP 表单同构），分别有连接/断开按钮和独立状态灯。
- SSE 快照 `snapshot()`：输出的每颗卫星带上 `role` 字段；KPI/矩阵/CN0 图要能区分或合并两通道（需确定，见待确认）。

**待确认**：
- 测站算 az/el 用的坐标来源？候选：①测站自己的 1005；②测站连接时填的 GGA/经纬度；③直接共用基站 `base_xyz`（同址或近距离时可接受）。这关系到测站没有 1005 时天空图能否画。
- 矩阵/CN0 柱状图是"两通道分开两块"还是"同表加 base/rover 标签列"？

---

### 项 2：天空图区分基站/测站，点大小代表 CN0（缩小一半），颜色代表星座

**需求理解**：同一张天空图上，基站卫星画**圆点**，测站卫星画**"十"字**；标记大小正比于 CN0，但整体比现在**小一半**；颜色仍按星座区分。

**改动点**（`drawSky()`）：
- 增加按 `role` 选择标记形状：base → `<circle>`（沿用），rover → "十"字（两条正交线段，或一个十字 `<path>`），颜色统一用 `SYS_COLOR[sys]`。
- 大小折半：现公式 `rad = 3 + (max(20,cn0)-20)*0.35`。"小一半"按减半理解，改为 `rad = (3 + (max(20,cn0)-20)*0.35) * 0.5` 之类；"十"字用同一尺度的半臂长。
- 图例补充：圆=基站、十=测站；星座颜色图例已有。
- 同一颗星两通道几乎同 az/el → 圆点上叠一个十字，视觉上正好可区分，无需做避让。

**待确认**：
- "比现在小一倍"= 减半（×0.5）？还是另有目标尺寸？请确认换算。
- 现有"近似环形"分支（无星历时）是否也要区分基站/测站？建议同样适配。

---

### 项 3：每 10 分钟为一个周期，保留最后一条 RTCM 记录 + 保留卫星当前位置记录

**需求理解**：引入"录制/抽稀"机制。按 10 分钟为一个周期，每个周期内：①保留该周期**最后一条 RTCM 记录**（原始报文）；②保留该时刻**每颗卫星的位置记录**（az/el/CN0 等轨迹点）。即把高频数据抽稀为"每 10 分钟一个快照"，作为项 4 导出的数据底座。

**改动点**（新增 `Recorder` 子系统）：
- 每通道一个录制器，随连接启动、断开停止。
- 周期划分：建议按时钟对齐的 10 分钟窗（与项目里 `gnss-period-coord` 的对齐口径一致），或按 GNSS TOW 对齐——需确定。
- 每周期落两类记录：
  - **RTCM 记录**：缓存本周期收到的最后一帧（或最后一组完整报文），保存原始字节；
  - **卫星位置记录**：周期边界时对当前 `sats` 做一次快照（sat, sys, prn, az, el, maxcn0 等）。
- 持久化：考虑到项 4 要导出 24 小时（24h/10min = 144 条/通道，量很小），但内存会随会话回收/重启丢失。**建议边产生边落盘**到本地文件，保证断电/重启后仍可导出。

**待确认**：
- "最后一条 RTCM 记录"的粒度：单帧？还是"最后一个完整历元的所有报文"？后者对回放/复算更有用。
- 周期对齐：墙钟 UTC 整 10 分 vs GNSS 时间对齐。
- 录制是否始终开启（只要连接就录），还是要一个"开始录制"开关？

---

### 项 4：选时间段/时长导出（如 24h），导出保留的 RTCM 并叠加轨迹；双通道按挂载点分文件夹保存

**需求理解**：提供导出功能——选择起止时间或时长（如 24h），把项 3 保留下来的 RTCM 记录导出，并把这段时间内的卫星轨迹**叠加**展示/导出。若基站、测站同时开启，则两路各自保存，并**以挂载点名称为文件夹**，把轨迹写到该文件夹下的文件中。

**改动点**：
- 导出 API：入参为起止时间（或时长），按通道把范围内的录制记录取出。
- 导出产物（每通道一套）：
  - 保留的 RTCM 记录合并成 `.rtcm` 文件；
  - 轨迹文件（建议 CSV：`time,sat,sys,az,el,cn0`，便于二次处理）；
  - 叠加视图：把范围内所有周期快照画在一张天空图上 → 每颗星 24h 的轨迹点连成轨迹。
- 目录规范（建议，待确认）：
  ```
  <导出根目录>/
    <base 挂载点名>/   ← 如 RTCM33/
        2026-06-16_2026-06-17.rtcm
        2026-06-16_2026-06-17_track.csv
    <rover 挂载点名>/
        ...
  ```
- 叠加视图载体：浏览器内"轨迹叠加"视图，还是导出为静态文件（PNG/HTML）？需确定。

**待确认**：
- 起止时间是绝对时间区间，还是"最近 N 小时"快捷选项，还是两者都要？
- 文件落在哪台机器、哪个根目录？（导出在运行 `app.py` 的服务端发生，文件落在服务端磁盘）
- 轨迹"叠加"是页面内交互查看，还是导出成图片/网页一并打包？
- 挂载点重名/文件回放(无挂载点名)时的文件夹命名兜底规则？

---

## 三、跨需求的架构影响与风险

- **`State` 双通道重构**牵动 `start/stop/snapshot/SSE payload/reap` 全链路，是本次最大改动面，建议先抽 `ChannelState` 再叠加录制。
- **持久化是新能力**：涉及文件生命周期、磁盘占用、并发会话/多浏览器隔离下的文件命名冲突、回收策略，需要单独设计，不只是"存个文件"。
- **测站坐标缺失**会导致测站天空图画不出（无 az/el），需在项 1 定坐标来源时一并解决。
- **会话回收**：当前 90 秒空闲就回收会话并停流，会中断录制。要 24h 连续录制，需让录制/连接的生命周期独立于浏览器在线状态（或拉长/取消该回收）。

---

## 四、建议的分期实施

1. **第 1 期（可视化先行，风险低）**：项 1 双通道接入 + 项 2 天空图基站/测站区分。先让两路能同时显示。
2. **第 2 期（录制底座）**：项 3 周期保留 + 落盘。
3. **第 3 期（导出与叠加）**：项 4 时间段导出 + 轨迹叠加 + 按挂载点分文件夹。

---

## 五、待你确认的问题清单（按优先级）

1. 测站(rover)算方位/高度角的坐标用哪一种？（自身 1005 / 连接时填的位置 / 共用基站坐标）
2. 天空图"比现在小一倍"是否就是尺寸减半（×0.5）？
3. 项 3"最后一条 RTCM 记录"是单帧还是最后一个完整历元的全部报文？周期按墙钟还是 GNSS 时间对齐？
4. 录制是"连接即录"还是需要显式开关？是否需要支持脱离浏览器的 24h 连续录制（即调整 90 秒会话回收策略）？
5. 项 4 导出：时间选择形式（绝对区间 / 最近 N 小时 / 两者）？导出根目录定在哪里？轨迹叠加是页面内查看还是导出成文件？
6. 矩阵与 CN0 柱状图在双通道下如何展示（分块 / 同表加角色列）？

---

*（天空图 10°/75° 常用高度角参考虚线已按你的要求直接实现，详见对话说明。）*
